import React from "react";
import "./style.css";

const Header = () => {
  return (
    <div onClick={() => window.scroll(0, 0)} className="header">
      🎥Entertaintenment Hub📸
    </div>
  );
};

export default Header;
